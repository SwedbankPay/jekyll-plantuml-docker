#!/bin/bash

# This config comes from Nokogiri error output as a suggestion
bundle config build.nokogiri --use-system-libraries

bundle check || bundle install

echo "Running: $@"

if [[ "$@" == "gh-pages-publish" ]]; then
  exec /usr/jekyll/bin/gh-pages-publish
else
  exec $@
fi
